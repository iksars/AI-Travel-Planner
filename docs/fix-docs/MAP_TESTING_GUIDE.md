# 地图功能快速测试指南

## 测试前准备

### 1. 确保服务运行

**后端：**
```bash
cd backend
npm run dev
# 应该看到：Ready on http://localhost:3000
```

**前端：**
```bash
cd frontend
npm run dev
# 应该看到：Local: http://localhost:5173
```

### 2. 配置检查

- [ ] OpenAI API Key 已配置（`backend/.env`）
- [ ] 高德地图 Key 已配置（`frontend/src/components/TravelMap.vue`）
- [ ] 用户已注册/登录

## 测试步骤

### 步骤 1: 生成带坐标的行程

1. 访问 http://localhost:5173
2. 登录系统
3. 点击"AI 智能生成"按钮
4. 填写表单：
   - **目的地**: 北京（或其他城市）
   - **出发日期**: 选择日期
   - **旅行天数**: 3
   - **总预算**: 5000
   - **人数**: 2
   - **旅行偏好**: 选择几个（如：美食、文化）

5. 点击"开始生成"
6. 等待 AI 生成（约 10-30 秒）
7. 生成成功后自动跳转到详情页

### 步骤 2: 查看地图

在详情页应该能看到：

#### 🗺️ 行程路线图卡片
- 地图容器（500px 高度）
- 顶部控制按钮：全部路线、第1天、第2天、第3天
- 地图上的彩色标记
- 连接各点的蓝色路线
- 底部图例（景点、酒店、餐厅、交通）

#### 地图交互测试

1. **查看全部路线**
   - 点击"全部路线"按钮
   - 应显示所有天数的标记和路线
   - 地图自动缩放以显示所有标记

2. **按天查看**
   - 点击"第1天"按钮
   - 只显示第1天的标记和路线
   - 地图重新聚焦到当天的位置

3. **点击标记**
   - 点击任意标记
   - 弹出信息窗口
   - 显示：名称、类型、时间、地址、描述

4. **地图操作**
   - 缩放：鼠标滚轮或双指缩放
   - 拖动：鼠标拖动或手指拖动
   - 旋转：右键拖动（3D 视图）

### 步骤 3: 验证数据

#### 标记验证
```javascript
// 在浏览器控制台
// 应该看到所有位置数据
console.log('Locations:', locations)
```

期望看到：
```javascript
[
  {
    name: "天安门广场",
    type: "attraction",
    coordinates: [116.397428, 39.903723],
    address: "北京市东城区...",
    time: "09:00",
    day: 1
  },
  // ...更多位置
]
```

#### 坐标格式
- 经度范围：-180 到 180
- 纬度范围：-90 到 90
- 北京大约：[116.4, 39.9]
- 上海大约：[121.5, 31.2]

### 步骤 4: 测试不同场景

#### 场景 1: 多天行程（5-7天）
- 测试更多标记的显示
- 验证按天筛选功能
- 检查路线连接

#### 场景 2: 不同城市
- 测试：上海、成都、西安、杭州
- 验证地图中心自动调整
- 检查标记位置准确性

#### 场景 3: 不同偏好
- 美食游：更多餐厅标记（黄色）
- 文化游：更多景点标记（红色）
- 休闲游：更多酒店标记（青色）

## 预期结果

### ✅ 成功标准

- [ ] 地图正常加载（3-5秒内）
- [ ] 所有标记显示正确
- [ ] 标记颜色与类型对应
- [ ] 路线连接所有标记
- [ ] 信息窗口正常弹出
- [ ] 按天筛选功能正常
- [ ] 地图可缩放和拖动
- [ ] 视野自适应所有标记

### ⚠️ 可能的问题

#### 问题 1: 地图空白
**现象**: 地图区域显示空白或加载失败

**检查**:
1. 浏览器控制台是否有错误
2. 高德地图 Key 是否配置
3. 网络是否可访问高德服务

**解决**:
```typescript
// 在 TravelMap.vue 中添加调试
console.log('Map loading...', AMAP_KEY)
console.log('Locations:', props.locations)
```

#### 问题 2: 缺少坐标
**现象**: 地图不显示或只有部分标记

**检查**:
```javascript
// 在详情页控制台
console.log('Map Locations:', mapLocations.value)
// 查看哪些位置缺少 coordinates
```

**解决**: 重新生成行程，确保 AI 返回坐标

#### 问题 3: 标记位置不准
**现象**: 标记位置偏离实际位置

**原因**:
- AI 生成的坐标有误差
- 地址描述不够准确

**解决**: 手动使用地理编码
```javascript
// 使用地理编码获取准确坐标
const coords = await mapRef.value?.geocode('北京市天安门广场')
console.log(coords)
```

#### 问题 4: 性能问题
**现象**: 地图加载慢或卡顿

**原因**:
- 标记点过多（>50个）
- 路线过于复杂

**解决**:
1. 使用按天筛选减少标记数
2. 启用标记聚合（见 MAP_FEATURE_GUIDE.md）

## 浏览器兼容性

### 支持的浏览器
- ✅ Chrome 90+
- ✅ Firefox 88+
- ✅ Safari 14+
- ✅ Edge 90+

### 不支持的浏览器
- ❌ IE 11 及以下
- ❌ 旧版移动浏览器

## 移动端测试

### 响应式布局
1. 打开浏览器开发者工具
2. 切换到移动设备视图
3. 测试不同屏幕尺寸：
   - iPhone SE (375px)
   - iPhone 12 (390px)
   - iPad (768px)

### 触摸操作
- 单指拖动：移动地图
- 双指缩放：缩放地图
- 点击标记：显示信息

## 调试技巧

### 1. 查看地图实例
```javascript
// 在 TravelMap.vue 中
console.log('Map instance:', map)
console.log('Markers:', markers)
```

### 2. 监听地图事件
```typescript
map.on('complete', () => {
  console.log('Map loaded successfully')
})

map.on('error', (error) => {
  console.error('Map error:', error)
})
```

### 3. 验证坐标数据
```javascript
// 检查坐标格式
locations.forEach(loc => {
  if (loc.coordinates) {
    const [lng, lat] = loc.coordinates
    if (lng < -180 || lng > 180 || lat < -90 || lat > 90) {
      console.error('Invalid coordinates:', loc.name, loc.coordinates)
    }
  }
})
```

## 性能测试

### 加载时间
- 地图 SDK 加载：< 3s
- 首次地图渲染：< 2s
- 标记添加：< 1s（50个标记内）

### 内存占用
- 空闲状态：< 50MB
- 显示 50 个标记：< 100MB
- 显示 100 个标记：< 150MB

## 报告问题

如果发现问题，请提供：

1. **问题描述**: 详细说明问题现象
2. **复现步骤**: 如何触发问题
3. **环境信息**:
   - 浏览器版本
   - 操作系统
   - 屏幕截图
4. **控制台日志**: 浏览器控制台的错误信息
5. **网络请求**: Network 标签页的请求状态

## 下一步测试

- [ ] 测试地理编码功能
- [ ] 测试不同地图样式
- [ ] 测试路线规划功能
- [ ] 测试离线地图
- [ ] 压力测试（大量标记）

## 成功示例

### 示例 1: 北京 3 日游
- 15 个景点标记
- 6 个餐厅标记
- 3 个酒店标记
- 平滑的路线连接
- 清晰的信息展示

### 示例 2: 上海 5 日游
- 25 个景点标记
- 10 个餐厅标记
- 5 个酒店标记
- 按天分组查看
- 信息窗口互动

## 结论

完成所有测试步骤后，地图可视化功能应该能够：
- ✅ 稳定加载和显示
- ✅ 准确标记位置
- ✅ 流畅的交互体验
- ✅ 清晰的信息展示
- ✅ 良好的性能表现

如有任何问题，请参考：
- [地图功能指南](./MAP_FEATURE_GUIDE.md)
- [配置指南](./CONFIGURATION.md)
- [故障排查](./CONFIGURATION.md#故障排查)
